const str = 'Koroedokan';

const firstChar = str.substring(0, 1);              // K
const midChars = str.substring(2, 3);               // r
const lastChars = str.substring(str.length - 2);    // an

console.log(firstChar + midChars + lastChars);      // Kran
